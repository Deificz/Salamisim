import React from "react";
import Intro from "../../components/intro";
type Props = {};

const intro_1 = (props: Props) => {
  return (
    <Intro title={"Socialize"} description={"Connect with other students in the country"} pageNum={1}/>
  );
};

export default intro_1;
