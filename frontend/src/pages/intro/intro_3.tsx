import React from "react";
import Intro from "../../components/intro";
type Props = {}

const intro_3 = (props: Props) => {
  return (
    <Intro title={"Need help?"} description={"Ask questions related to your academic problems !"} pageNum={3}/>
  );
}

export default intro_3

